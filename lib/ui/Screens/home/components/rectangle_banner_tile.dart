import 'package:flutter/material.dart';



class GymsNearWidget extends StatelessWidget {
  const GymsNearWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Row(
          children: [
            Text('Popular ')
          ],
        )
      ],
    );
  }
}
